package com.termux.app.terminal.split;

import java.io.Serializable;

/**
 * Clase base abstracta para el árbol de splits.
 * Un SplitNode puede ser un LeafNode (una terminal) o un BranchNode (una división).
 */
public abstract class SplitNode implements Serializable {
    // Marker interface
}
